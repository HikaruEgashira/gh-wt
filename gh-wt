#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "$SCRIPT_DIR/lib/env.sh"
source "$SCRIPT_DIR/lib/backend.sh"
source "$SCRIPT_DIR/lib/cache.sh"
source "$SCRIPT_DIR/lib/deps.sh"
source "$SCRIPT_DIR/lib/overlay.sh"
source "$SCRIPT_DIR/lib/worktree.sh"

usage() {
    cat <<'EOF'
Usage:
  gh wt add <branch> [path]  Create a worktree (CoW where the platform supports it)
  gh wt list                 List worktrees (wraps git worktree list)
  gh wt remove               Select a worktree via fzf and remove it
  gh wt gc                   Delete unreferenced cache entries
  gh wt doctor               Check that the tool can operate on this host
  gh wt -- <command>         Run <command> inside the selected worktree
  gh wt <command>            Run <command> with the selected worktree path as arg

The implementation auto-selects the best available strategy per platform
(Linux OverlayFS, macOS APFS clonefile, or a plain git worktree fallback).
There is no user-facing knob — it just works.
EOF
}

cmd="${1:-}"
case "$cmd" in
    "" | "-h" | "--help")
        usage
        ;;
    add)
        shift
        require_env
        cmd_add "$@"
        ;;
    list)
        require_env
        cmd_list
        ;;
    remove | rm)
        require_env
        cmd_remove
        ;;
    gc)
        require_env
        cmd_gc
        ;;
    doctor)
        cmd_doctor
        ;;
    --)
        shift
        require_env
        cmd_exec_in "$@"
        ;;
    *)
        require_env
        cmd_exec_with "$@"
        ;;
esac
